<%= render 'shared/title', title: "All Supported Schools" %>

<% if flash[:notice] %>
  <p class="notice" style="color:red;"><%= flash[:notice] %></p>
<% end %>
</br>

<h5>This is a list of all the schools currently supported by the site. If you don't see your school, please register and get your school connected!</h5>
</br>

<div class="container">
  <div class="row">
    <div class="col-md-3">
    </div>

    <div class="col-md-6">

      <div class="well" style="background:white;">

				<% @schools.each do |s| %>
					<p><%= s.name %></p>
				<% end %>


			</div>
      <%= will_paginate @schools, renderer: BootstrapPagination::Rails %>
    </div>
    <div class="col-md-3">
    </div>
  </div>
</div>