<h2>All Students:</h2>

<% @users.each do |u| %>
	<%= gravatar_image_tag(u.email) %>
	<p><%=u.username %></p>
	<p><%= link_to "Add As Friend", friendships_path(:friend_id => u), :method => :post %></p>
<% end %>